{
  "name": "vevui",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n user,admin -c blue,green \"npm run dev:user\" \"npm run dev:admin\"",
    "dev:user": "cd ve-vui-user && npm run dev",
    "dev:admin": "cd ve-vui-admin && npm run dev",
    "build": "npm run build:user && npm run build:admin",
    "build:user": "cd ve-vui-user && npm run build",
    "build:admin": "cd ve-vui-admin && npm run build"
  }
}
